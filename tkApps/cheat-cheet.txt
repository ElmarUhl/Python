Programa Estrutura Geral

from tkinter import * - importar tudo

name_class = Tk()
{ Código }
name_class.mainloop()

name_class.title("Texto") - Título da janela
name_class.geometry("widthxheight+x+y") - definição de geometria da janela
menu.geometry("%dx%d+%d+%d" %(width, height, posx, posy))
name_class.resizable(True,False) - permissões de redimensionamento da janela
name_class['bg'] = 'yellow' - cor de fundo da janela

button = Button(menu,text="Executar", command= lambda: cmd_click("Olá")) - cria botão associado ao comando
button.pack() - empacotamento

texto = StringVar()
texto.set("texto na variavel")
number = float(textBox1.get())

label1 = Label(menu,text="Label 1") - label
label1 = Label(menu,text="label",bg="yellow",fg="#ff0000",font="Arial 20 bold italic",width=40)
label1 = Label(menu,text="label",font="Arial 12",bd=1,relief="solid").pack()
label = Label( menu,text="Label",font="Arial 20",bd=1,relief="solid",padx=10,pady=10)
label1 = Label(menu,text="label1\nlabel1",bd=1,relief="solid",justify=LEFT,anchor=W,width=50)
label = Label(menu,text="label\nlabel",font="Arial 20",bd=1,relief="solid",width=25,height=4,anchor=CENTER).pack()
label1 = Label(menu,bg="red",fg = "white",textvariable=texto)

label1.grid(row=0,column=0)
Label(menu, width=20, bg="red").grid(column=0)
Label(menu, bg="green").grid(columnspan=2, sticky="we")
Label(root, textvariable=final)

t = Entry(root) - caixa de entrada
l["text"] = t.get() - coloca na label o texto da caixa de entrada t
final.set(str(round(C,1)) + " graus Celsius")

t1.focus()

def cmd_click(mensagem): - função
    print(mensagem)

widthScreen = menu.winfo_screenwidth() - largura da tela
heightScreen = menu.winfo_screenheight() - altura da tela

class FrameNome(Frame):
    def __init__(self, meumaster):
        super().__init__()
        self['height'] = 150
        
        label_nome = Label(self,text="Nome: ")
        label_nome.grid(row=0, column=0)

frame_nome1 = FrameNome(root).grid()

valor_check = IntVar()
valor_check.get()
Checkbutton(root, text='Esta é uma checkbox', variable=valor_check, command=apresentar)
